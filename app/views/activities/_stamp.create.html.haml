.content
  %span.date
    = time_ago_in_words(activity.created_at)
    ago
  created a
  = link_to activity.trackable_type, activity.trackable
  of
  - if activity.trackable_type == "Stamp::Label"
    = link_to activity.recipient.name, domain_path(activity.recipient.name)
  - elsif activity.trackable_type == "Stamp::Flag"
    = link_to activity.recipient.name, app_path(activity.recipient)
  - else
    wtf how does this work
  its
  = activity.trackable.state
  

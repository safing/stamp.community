- if object.errors.count > 0
  %div.ui.attached.error.message
    .header
      Invalid
      = object.class.name.titleize
    %ul.list
      - object.errors.full_messages.each do |message|
        %li=message
